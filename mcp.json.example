{
  "mcpServers": {
    "echo": {
      "command": "python",
      "args": ["-c", "import sys, json; print(json.dumps({'tools': [{'name': 'echo', 'description': 'Echos input', 'inputSchema': {'type': 'object', 'properties': {'msg': {'type': 'string'}}}}]}));"],
      "transport": "stdio"
    }
  }
}
