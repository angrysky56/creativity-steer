# Ollama host (default shown). Only needed if not localhost.
OLLAMA_HOST=http://localhost:11434

# Default models used by the Ollama backend.
CREATIVITY_STEER_GEN_MODEL=gemma4:12b
CREATIVITY_STEER_EMBED_MODEL=embeddinggemma:latest
